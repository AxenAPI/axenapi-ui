<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>
<?import javafx.scene.control.TableView?>
<?import javafx.scene.control.TableColumn?>
<?import javafx.scene.canvas.Canvas?>
<?import javafx.scene.control.Slider?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>

<?import java.net.URL?>
<?import javafx.scene.SubScene?>
<?import javafx.collections.ObservableList?>
<BorderPane xmlns="http://javafx.com/javafx/8" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="org.example.demojavafx.MainWindow">
    <stylesheets>
        <URL value="@styles.css" />
    </stylesheets>
    <top>
        <MenuBar>
            <Menu text="File">
                <!-- Add file options here -->
            </Menu>
            <Menu text="Help">
                <!-- Add help options here -->
            </Menu>
        </MenuBar>
    </top>
    <center>
        <SplitPane>
            <VBox alignment="CENTER" spacing="10">
                <Label text="Choose files to be used to draw the event graph" styleClass="form-label" />
                <HBox alignment="CENTER" spacing="10">
                    <VBox alignment="CENTER" spacing="10">
                        <Button text="Choose Files" onAction="#chooseFiles" styleClass="form-button"/>
                        <Button text="Add Service" onAction="#addService" styleClass="form-button"/>
                    </VBox>
                    <VBox alignment="CENTER" spacing="10">
                        <Button text="Add topic(queue)" onAction="#addTopic" styleClass="form-button"/>
                        <Button text="Add Event" onAction="#addEvent" styleClass="form-button"/>
                        <Button text="CreateLink" onAction="#createLink" styleClass="form-button"/>
                    </VBox>
                </HBox>
                <TableView fx:id="fileInfoTable" styleClass="custom-table-view">
                    <columns>
                        <TableColumn text="File Name" styleClass="table-column"/>
                        <TableColumn text="Title" styleClass="table-column"/>
                        <TableColumn text="Select" styleClass="table-column"/>
                    </columns>
                </TableView>
                <Button text="Export Specification" onAction="#exportSpecification" styleClass="form-button"/>
            </VBox>
            <StackPane fx:id="pane">
                <VBox alignment="BOTTOM_RIGHT" spacing="10" styleClass="zoom-panel">
                    <Slider fx:id="zoomSlider" min="1.0" max="5.0" value="1.0" showTickLabels="true" showTickMarks="true" orientation="VERTICAL" />
                    <Button onAction="#resetZoom" styleClass="reset-button">
                        <graphic>
                            <ImageView fitWidth="20" fitHeight="20">
                                <Image url="@reset-icon.png"/>
                            </ImageView>
                        </graphic>
                    </Button>
                </VBox>
            </StackPane>
        </SplitPane>
    </center>
</BorderPane>